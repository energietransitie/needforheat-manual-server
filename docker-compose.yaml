version: "3.8"
services:
  web:
    container_name: twomes-manual-server
    build: .
    ports:
      - 8080:8080
    volumes:
      - ./source:/source
    environment:
      - NFH_MANUAL_SOURCE=./source
      - NFH_FALLBACK_LANG=en-US